version: '3.8'

services:
  l484-app:
    build: 
      context: .
      dockerfile: Dockerfile.simple-static
    container_name: l484-innovation-platform
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production